<%= render 'layouts/shared/page_title', title: Company.model_name.human(count: 2), pretitle: t(:edit) %>

<div class="page-body">
  <div class="container-xl">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= @company.name %></h3>
      </div>
      <div class="card-body">
        <%= render 'companies/partials/form', company: @company, method: :patch %>
      </div>
    </div>
  </div>
</div>

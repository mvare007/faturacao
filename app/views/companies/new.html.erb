<%= render 'layouts/shared/page_title', title: Company.model_name.human(count: 2) %>

<div class="page-body">
  <div class="container-xl">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= t(:new_female) %> <%= Company.model_name.human %></h3>
      </div>
      <div class="card-body">
        <%= render 'companies/partials/form', company: @company, method: :post %>
      </div>
    </div>
  </div>
</div>
